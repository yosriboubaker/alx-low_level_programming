#include "main.h"
/**
 * main - entry point & prints the program name
 * @argc: Argument Count
 * @argv: Argument Vector
 * Return: Always 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
